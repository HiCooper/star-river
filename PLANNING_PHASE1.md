<!--
  Planning Spec
  Generated by: Qoder /planning skill
  Date: 2026-05-20
  Status: Draft
-->

# Plan: Build Hydra-Wall Phase 1 Backend & Connect Frontend to Real APIs

**Complexity:** Large — 从零搭建 Go 后端服务（含数据库、认证、8 个核心模块 CRUD + 评估引擎 + 事件系统），同时改造两个 Next.js 前端从 mock 数据切换为真实 API 调用。

## Goal
Enable developers and operators to manage paywall campaigns, audiences, and configurations through real backend APIs so that the frontend admin console can persist data and SDK clients can evaluate placements and report events.

## Deliverables

### Backend (wall-service)
- **Go 项目脚手架**: `hydra-wall/service/` 目录，标准布局（cmd/、internal/、pkg/、api/、config/），Makefile，Dockerfile，`.env.example` → 提供可运行的服务基础框架
- **Docker Compose 环境**: `docker-compose.yml` 编排 PostgreSQL + Redis + wall-service → 一键启动完整本地开发环境
- **数据库迁移**: GORM + gooose/golang-migrate，8 张核心表（apps、campaigns、placements、audiences、paywalls、audience_paywalls、transactions、events）DDL 脚本 → 数据持久化基础
- **认证中间件**: API Key 鉴权中间件（`X-API-Key` 请求头）+ JWT Token 用于管理后台 → 安全访问控制
- **统一错误处理**: 标准错误码 + 错误响应格式 → 一致的 API 体验
- **App CRUD API**: `GET/POST/PUT/DELETE /api/v1/apps` + `POST /api/v1/apps/:id/rotate-key` → 应用生命周期管理
- **Campaign CRUD API**: `GET/POST/PUT/DELETE /api/v1/campaigns` + activate/deactivate → 付费活动管理
- **Placement API**: `GET/POST /api/v1/campaigns/:id/placements` + `DELETE /api/v1/placements/:id` → 事件触发点绑定
- **Audience API**: `GET/POST /api/v1/campaigns/:id/audiences` + `PUT /api/v1/audiences/:id` + reorder → 目标用户规则定义
- **Paywall API**: `GET/POST/PUT/DELETE /api/v1/paywalls` → 付费墙配置管理
- **Audience-Paywall 关联 API**: `POST/PUT/DELETE /api/v1/audiences/:id/paywalls` → 流量分配管理
- **Placement 评估 API**: `POST /api/v1/evaluate` → SDK 核心调用，匹配 Campaign→Audience→Paywall
- **事件上报 API**: `POST /api/v1/events` 批量上报（6 种事件类型），Redis 队列异步写入 → 数据采集
- **Analytics 聚合 API**: `GET /api/v1/analytics/overview`、`/campaigns`、`/paywalls` + `GET /api/v1/transactions` → Dashboard 数据源

### Frontend (wall-admin)
- **API Client 模块**: `src/lib/apiClient.ts` 统一管理所有 API 调用，封装 fetch + 错误处理 + 认证头 → 前端 API 调用基础
- **Dashboard 页面对接**: 替换硬编码数据为 Analytics API 响应 → 真实数据展示
- **Paywall 页面对接**: 替换 mock 为 Paywall CRUD API 调用 → 付费墙管理
- **Experiment 页面对接**: 替换 mock 为 Experiment CRUD API 调用 → 实验管理
- **Rules 页面对接**: 替换 mock 为 Audience CRUD + 关联 API 调用 → 定向规则管理
- **Settings 页面对接**: 替换 mock 为 App CRUD + API Key 管理 → 应用设置
- **全局 Loading/Error/Empty 状态**: 统一的加载、错误、空状态处理 → 用户体验

### Infrastructure
- **健康检查端点**: `GET /health` → 服务可用性检测
- **Prometheus 指标**: `GET /metrics` → 请求量、延迟、错误率监控

**Out of scope:**
- A/B 实验统计推断引擎（p-value、置信区间计算）
- 付费墙可视化拖拽编辑器（当前已有静态模板）
- Hydra-Pay 商品数据对接
- 客户端 SDK 开发（iOS/Android/Web/Flutter）
- CI/CD 自动化部署
- Webhook 推送
- 多租户/团队管理/RBAC

## Acceptance Criteria

```
AC-1  [Happy path]     Given 一个新建的 App，When 运营人员通过 wall-admin 创建 Campaign → Placement → Audience → Paywall 并关联，Then 所有数据成功写入 PostgreSQL 并在列表页正确展示
AC-2  [Placement 评估]  Given SDK 调用 POST /api/v1/evaluate {placement, user_id, attributes}，When Audience 规则匹配，Then 返回 {action: "show_paywall", paywall: {...}}；当无匹配时返回 {action: "execute_feature"}
AC-3  [事件上报]        Given SDK 批量上报事件 POST /api/v1/events（最多 50 条），When 请求成功，Then 事件写入 Redis 队列并在 5s 内异步写入 PostgreSQL
AC-4  [认证拦截]        Given 请求不带 X-API-Key 或携带无效 Key，When 访问受保护 API，Then 返回 401/403 + 标准错误码
AC-5  [校验错误]        Given Audience 条件 JSON 非法 / Percentage 总和 != 100 / Placement event_name 重复，When 创建或更新，Then 返回 400/409 + 对应错误码
AC-6  [前端对接]        Given wall-admin 所有页面（Dashboard/Paywall/Experiment/Rules/Settings），When 执行 CRUD 操作，Then 数据持久化到后端且列表自动刷新，无 mock 数据残留
AC-7  [非功能性]        简单查询 API 响应时间 P95 < 200ms；评估 API 响应时间 P95 < 100ms（含 Redis 缓存命中）；单元测试覆盖率 > 70%
```

## Implementation Path

### Phase 1: 基础设施与数据库（Week 1）
1. **项目脚手架搭建** — 创建 Go + Gin 项目结构、Makefile（build/run/test/lint/migrate）、Dockerfile、`.env.example` → 确立开发基础
2. **Docker Compose 环境** — 编排 PostgreSQL 16 + Redis 7 + wall-service → 一键启动本地环境
3. **数据库连接与迁移** — GORM 连接池配置 + goose 迁移工具 + 8 张表 DDL 脚本 → 数据层就绪
4. **GORM Model 定义** — 所有表的 Go struct 模型（含标签、索引、外键关系） → ORM 映射层
5. **健康检查 + Prometheus 指标** — `/health` 和 `/metrics` 端点 → 可观测性基础

### Phase 2: 认证与通用层（Week 2）
6. **统一错误处理** — 错误码定义 + 中间件统一格式响应 → 一致的错误体验
7. **API Key 认证中间件** — `X-API-Key` 验证 + 数据库查询 → 安全访问控制
8. **JWT 认证中间件** — wall-admin 登录 JWT 签发与验证 → 管理后台认证
9. **请求日志中间件** — method/path/status/duration 日志 + 慢查询日志 → 可观测性

### Phase 3: 核心 CRUD API（Week 2-3）
10. **App CRUD** — 创建/读取/更新/删除 + API Key 生成/轮换 → 应用管理
11. **Campaign CRUD** — 创建/读取/更新/删除 + 激活/停用状态切换 → 活动管理
12. **Placement API** — 添加/移除 Placement + event_name 唯一性校验 → 触发点管理
13. **Audience API** — 创建/读取/更新/删除 + 条件 JSON 校验 + reorder → 定向规则
14. **Paywall API** — 创建/读取/更新/删除 + template 枚举校验 + config JSON 校验 → 付费墙管理
15. **Audience-Paywall 关联 API** — 关联/更新百分比/取消 + percentage 总和 = 100 校验 → 流量分配

### Phase 4: 评估引擎与事件系统（Week 3-4）
16. **Placement 评估引擎** — 匹配 Campaign → 评估 Audience（按 sort_order）→ 选择 Paywall（按 percentage 分配）→ 返回决策结果 → SDK 核心接口
17. **Audience 条件评估 DSL** — 实现条件表达式解析器（field/operator/value，AND/OR 分组）→ 规则匹配核心
18. **事件上报 API** — 接收批量事件 → 写入 Redis 队列 → 校验事件类型 + 数量限制（≤50）→ 数据采集入口
19. **事件异步 Worker** — 消费 Redis 队列 → 写入 PostgreSQL events 表 → 失败重试机制 → 数据持久化

### Phase 5: Analytics 与前端对接（Week 4-5）
20. **Analytics 聚合 API** — MRR/ARR/转化率聚合查询 + 按 Campaign/Paywall 维度聚合 + 时间范围过滤 → Dashboard 数据源
21. **Transaction 记录 API** — 交易列表查询 + 分页 → 最近交易展示
22. **wall-admin API Client** — 创建 `apiClient.ts` 模块，封装所有 API 调用 → 前端 API 基础
23. **Settings 页面对接** — App CRUD + API Key 管理 → 最先对接，为后续提供 API Key
24. **Paywall 页面对接** — Paywall CRUD → 付费墙管理
25. **Rules 页面对接** — Audience CRUD + 关联管理 → 定向规则
26. **Experiment 页面对接** — Experiment CRUD → 实验管理
27. **Dashboard 页面对接** — Analytics Overview + Transactions → 数据展示

### Phase 6: 测试与验证（Week 5）
28. **单元测试** — 核心业务逻辑（评估引擎、认证中间件、校验逻辑）→ 覆盖率 > 70%
29. **集成测试** — API 端到端测试（创建 App → Campaign → Placement → Audience → Paywall → 评估 → 事件上报）→ 全链路验证
30. **前端对接验证** — 所有页面 CRUD 操作手动测试 + Loading/Error/Empty 状态验证 → 前端质量保证

## Open Questions

1. **Audience 条件 DSL 格式** — 采用 `[{field, operator, value}, ...]` 数组 + `{operator: "AND/OR", conditions: [...]}` 嵌套结构（假设，待评审）
2. **wall-admin 登录认证** — MVP 阶段使用简单 JWT（用户名/密码硬编码），后续接入 OAuth（假设）
3. **API 限流** — Phase 1 不做 rate limiter，Phase 2 引入（假设）
4. **事件上报批量压缩** — 不支持 gzip 压缩，仅支持 JSON 批量上报（假设）
